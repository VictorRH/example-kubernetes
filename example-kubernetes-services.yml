apiVersion: v1
kind: Service
metadata:
  name: example-kubernetes-service
  labels:
    name: example-kubernetes-service
    app: example-kubernetes
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: example-kubernetes
    type: example-kubernetes